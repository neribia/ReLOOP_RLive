[project]
name = "rlive-common"
version = "0.1.0"
description = "Core functionality for ReLoop RLive"
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = [
    "numpy>=2.2.6",
    "pydantic>=2.12.3",
]

[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"
